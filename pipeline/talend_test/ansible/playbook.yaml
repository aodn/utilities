---
- name: test talend
  hosts: "{{ hosts }}"
  gather_facts: no

  roles:
    - process_files
    - check_results

  vars_files:
    - global_vars.yaml

  vars:
    harvester_timeout: "{{ timeout|default(default_timeout) }}"
