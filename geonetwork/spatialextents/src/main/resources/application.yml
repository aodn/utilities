# Connect to production read only replica.
spring:
  datasource:
    url: "jdbc:postgresql://db-read2-harvest-prod.aodn.org.au:5432/harvest"
    username: geoserver_portal
    password: ${havesterPwd}

geonetwork:
  gnPassword: ${geonetworkPwd}